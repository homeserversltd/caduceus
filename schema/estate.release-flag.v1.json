{
  "schema": "estate.release-flag.v1",
  "schema_version": "1.0.0",
  "role": "Immutable component release verification flag",
  "authority": {
    "owner": "caduceus",
    "path": "schema/estate.release-flag.v1.json",
    "producer": "ci/release-flag.py",
    "consumers": [
      "Caduceus release readers",
      "ci/release-flag.py"
    ],
    "source_docs": [
      "harmonia-component-release-identity-law",
      "harmonia-beam-syzygy-law"
    ]
  },
  "description": "The immutable release.flag asset for a producing component. The five-field kernel also admits interpreted components; env_sha and sha256 are optional producer-declared evidence.",
  "fields": {
    "schema": {
      "type": "string",
      "const": "estate.release-flag.v1"
    },
    "component": {
      "type": "string"
    },
    "source_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "flagged_at": {
      "type": "string"
    },
    "pipeline_url": {
      "type": "string"
    },
    "env_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "64-hex digest whose input the producing component declares in its own release documentation; for caduceus, the compact JSON of the per-profile digest map in publisher PROFILES order; for a single-binary component, that binary."
    }
  },
  "field_types": {
    "schema": "literal:estate.release-flag.v1",
    "component": "string",
    "source_sha": "string",
    "env_sha": "string",
    "sha256": "string",
    "flagged_at": "string",
    "pipeline_url": "string"
  },
  "required": [
    "schema",
    "component",
    "source_sha",
    "flagged_at",
    "pipeline_url"
  ],
  "optional": [
    "env_sha",
    "sha256"
  ],
  "evolution": {
    "law": "additive-only",
    "missing_field_semantics": "Missing frozen kernel fields are refused; absent optional fields mean no evidence",
    "unknown_fields_preserved": true
  },
  "canonical_bytes": "UTF-8 JSON, two-space indentation and one trailing LF. GET schema serves these exact file bytes."
}
